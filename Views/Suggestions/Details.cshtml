@model VictuzWeb.Models.Suggestion

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Suggestie</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Naam
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            Beschrijving
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
        <dt class = "col-sm-2">
            Aanmaakdatum
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CreatedAt)
        </dd>
    </dl>
</div>
<div>

    @if (User.IsInRole("Admin"))
    {
        <a asp-action="SuggestionToGathering" asp-route-id="@Model.Identifier">Maak Gathering</a>
        @: |
    }
    <a asp-action="Edit" asp-route-id="@Model?.Identifier">Bewerken</a> |
    <a asp-action="Index">Terug naar lijst</a>





        @if(Model.Likes.Count() == 0)
        {
            <form asp-action="like">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="Identifier" />
                <div class="form-group">
                    <input type="submit" value="Like" class="btn btn-primary" style="margin-top:5px" />
                </div>
            </form>
        }
        @if (Model.Likes.Count() != 0)
        {
        <form asp-action="RemoveLike">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Identifier" />
            <div class="form-group">
                <input type="submit" value="Like weghalen" class="btn btn-primary" style="margin-top:5px" />
            </div>
        </form>
        }




</div>
